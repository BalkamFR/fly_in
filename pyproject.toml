[project]
name = "fly-in"
version = "0.1.0"
description = "Fly in with pygame"
requires-python = ">=3.11,<3.14"
dependencies = [
    "pygame>=2.0.0",
]

[dependency-groups]
dev = [
    "mypy>=1.0.0",
    "flake8>=6.0.0",
]
